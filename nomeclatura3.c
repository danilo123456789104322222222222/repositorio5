#include <stdio.h> // erro sem espaçamento

int main(){
   int numero = 5; // erro: sem definição de var
   printf("O valor de numero e: %d\n", numero);
   numero = 10;
   printf("O novo valor de munero e: %d\n", numero);
   return 0;
}